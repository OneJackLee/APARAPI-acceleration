#Use only for first time
git clone https://github.com/OneJackLee/APARAPI-acceleration

#Before adding/changing code
git pull

#Once changes made, want to add to git folder
git add *
git commit -m "commit message"
git status
git push origin master
